ZCV_collapsing_union = {

    ZCV_ZCX = {

        icon = GFX_decision_revolt
        activation = {
           NOT = {
            has_completed_focus = ZCV_reunite_nation
           }
        }

        available = {
           always = no 
        }

        cancel_trigger = {
            OR = {
                ZCX = {exists = yes}
                has_completed_focus = ZCV_reunite_nation
            }
        }
        days_mission_timeout = 45

        fire_only_once = yes
        
        timeout_effect = {
            ZCV = {
             country_event = ZCV.2
            }
        }
    }
    ZCV_ZCC = {

        icon = GFX_decision_revolt
        activation = {
           NOT = {
            has_completed_focus = ZCV_reunite_nation
           }
        }

        available = {
           always = no 
        }

        cancel_trigger = {
            OR = {
                ZCC = {exists = yes}
                has_completed_focus = ZCV_reunite_nation
            }
        }
        days_mission_timeout = 163

        fire_only_once = yes
        
        timeout_effect = {
            country_event = ZCV.3
        }
    }
    ZCV_ZCZ = {

        icon = GFX_decision_revolt
        activation = {
           NOT = {
            has_completed_focus = ZCV_reunite_nation
           }
        }

        available = {
           always = no 
        }

        cancel_trigger = {
            OR = {
                ZCZ = {exists = yes}
                has_completed_focus = ZCV_reunite_nation
            }
        }
        days_mission_timeout = 314

        fire_only_once = yes
        
        timeout_effect = {
            country_event = ZCV.4
        }
    }
}

debug_decisions = {
    ZCV_tree = {
        available = {
           tag = ZCV
        }
        complete_effect = {
            load_focus_tree = wighardian_cw_tree
            mark_focus_tree_layout_dirty = yes
        }
    }
}