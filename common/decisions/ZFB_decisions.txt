ZFB_civil_war_cat = {

    ZFB_end_of_civil_war = {

        icon = generic_civil_support

        available = {
            NOT = { country_exists = ZFA }
            NOT = { country_exists = ZFK }
            NOT = { country_exists = ZFS }

            controls_province = 10337

        }

        fire_only_once = yes

        complete_effect = {
            country_event = ZFB_cw_events.1
        }
    }
}