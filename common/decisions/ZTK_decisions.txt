ZTK_grand_exhibit_category = {

	ZTK_grand_exhibit_state_decision = {
		icon = generic_nationalism

		target_trigger = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				is_core_of = ROOT
				state_population_k > 1000
				state_and_terrain_strategic_value > 64
			}
		}

		state_target = yes

		highlight_states = {
           	
    	}

		fire_only_once = yes
        cost = 10
		days_remove = 10


		remove_effect = {
			add_popularity = {
                ideology = communism
                popularity = 0.025
            }
		}
		cancel_trigger = { FROM = { NOT = { is_owned_by = ROOT } } }
		ai_will_do = {
			base = 25
		}
	}
}
