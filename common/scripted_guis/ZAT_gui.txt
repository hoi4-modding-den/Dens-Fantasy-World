#scripted_gui = {
#	
#	ZAT_welcome_message = {   ##Controls the main window of the welcome message, remember to credit me in the loc if you're directly copying this stuff
#		context_type = player_context
#		#parent_window_token = politics_tab
#		window_name = "ZAT_welcome_window"  ##Change the names of all windows, defined in interface/*.gui
#
#		visible = {
#			has_country_flag = ZAT_welcome_message_flag  ##change the flag names
#		}
#		effects = {	
#			ZAT_welcome_message_close_click = {  ##make sure the button names match 
#				clr_country_flag = ZAT_welcome_message_flag
#				clr_country_flag = ZAT_credits_visible_flag
#			}
#			ZAT_welcome_message_continue_click = {
#				clr_country_flag = ZAT_welcome_message_flag
#				clr_country_flag = ZAT_credits_visible_flag
#			}
#			ZAT_welcome_message_credits_click = {
#				if = {
#					limit = {
#						NOT = { has_country_flag = ZAT_credits_visible_flag }
#					}
#					set_country_flag = ZAT_credits_visible_flag
#				}
#				else = {
#					clr_country_flag = ZAT_credits_visible_flag
#				}
#			}
#		}
#	}
#	ZAT_credits_main_window = {  ##Controls the credits sub-window
#		context_type = player_context
#		window_name = "ZAT_credits_main_window"
#		visible = { has_country_flag = ZAT_credits_visible_flag }
#		
#		
#	}
#}

scripted_gui = {
	ZAT_celestial_balance_decision_ui = {
		context_type = decision_category
		window_name = "ZAT_celestial_balance_decision_ui_window"

		ai_enabled = {
			always = no
		}
		
        effects = {
            ZAV_open_notebook_button_click = {
                if = {
                    limit = {
                        has_country_flag = ZAV_open_notebook
                    }
                    
                    clr_country_flag = ZAV_open_notebook

                }
                else = {
                    set_country_flag = ZAV_open_notebook
                }
            }
        }

		properties = {  
			# LOYALTY PROGRESS BARS NEED TO BE HERE
			#ZAT_generalitat_faction_progress_bar = {
			#	frame = ZAT.ZAT_generalitat_faction_progress_bar
			#}									
		}	
	}		
}